"use client";
export const MainProducts = () => {
  console.log("SHOPIFY_HOST_NAME", process.env.NEXT_PUBLIC_SHOPIFY_HOSTNAME);

  return (
    <section>
      <h1>Main Products</h1>
    </section>
  );
};
